# 本地验证记录 · 2026-09-24

在原项目静态服务器和 Codex 内置浏览器执行。

- 88 项浏览器断言通过：17 条路径的理想描画、手部偏差、明显偏离、反向；逐笔输入事件、撤销/重做、恢复、17 笔完整流程与阶段回顾；三模式独立保存；390px 手指与 768px pen 坐标；清除当前笔、保留其他笔、跳过标记与恢复；touch-action；三层纸上页与完成勾选；PNG 内容生成。
- 原生鼠标 drag 从肩部起点到终点：得到“肩线连接了两个定位点”，继续按钮启用。
- 实际点击作品导出：产生 PNG 下载链接。
- 390px/768px iframe 视觉检查完成，窄屏提示显示在画板上方。
- JavaScript 语法与 git diff whitespace 检查通过。
- 没有向生产账户提交测试照片；现有 FEISubmissions 接口的线上权限/网络需要真实登录环境验证。
- 没有真实 iPad/Apple Pencil 硬件，触控和 pen 验证使用浏览器 PointerEvents；压感不是本版本功能。
- 尚未推送或发布。

测试页面只在 localhost 允许执行，不应在学生登录会话中使用。合成事件仅在测试中覆写 SVG pointer capture，生产代码仍使用真实浏览器指针捕获。

## 英文与视觉统一更新

课程学生可见内容全部改为英文；统一为 Scene Drawing / Cardboard 现有课程的渐变背景、白色圆角容器和青色主按钮。88 项交互回归再次通过，另检查英文手机布局与纸上练习控件。原有笔画与本机进度保留。

## 教师参考图顺序修订 v2

147 项浏览器断言通过，覆盖四组定位、36 条笔画、承重踝定位、头部两笔合为 Step 1、肩腰臀先后、腿后颈部、各阶段回顾、撤销重做、恢复与导出。真实鼠标点击头顶和下巴定位后显示完成并解锁下一组。旧 v1 作品在 Earlier demo artwork 中保留，并已实际验证 PNG 导出。手机宽度定位界面可操作。

两个教师 HEIC 文件只转换为临时预览供观察，没有放入网站资源或从照片提取坐标。精确比例与腿部术语/透视顺序仍待教师校对。尚未收到视频，已接好逐笔视频起止时间字段和片段播放器；没有伪造教师示范视频。
