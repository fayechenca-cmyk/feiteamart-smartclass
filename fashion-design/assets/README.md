# 教师正式素材预留

建议文件：`standing-approved.svg`、`standing-final.png`、`stroke-01.mp3` 等。
在 `../course-data.js` 的 `assets` 与每步 `audio` 中填写路径和来源/授权。
SVG 路径需要按笔拆分并统一到 lesson.canvas 坐标，不能直接从倾斜参考照片自动抽取比例。
当前目录未包含未经教师确认的正式素材。
