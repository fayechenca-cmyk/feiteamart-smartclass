/* ============================================================
 * Scene Drawing Foundation · Unit 01 · Lesson 01 — Extreme Wide Shot
 * Lesson content data.
 *
 * SCAFFOLD ONLY (2026-09-05) — deliberately empty. The real per-stage
 * copy, image assets, story beats, and term definition come from
 * Lesson01-ExtremeWideShot-Spec-v1.0.md, which was referenced in the
 * build prompt as "attached alongside this prompt" but was not
 * actually present anywhere in the repo or the conversation at build
 * time. Filling this in from guesswork would risk baking a wrong
 * "Jojo"/story/term narrative into a file meant to set the content
 * pattern for lessons 02-05 of this unit — flagged instead of guessed.
 *
 * See index.html's own top-of-file comment for the same flag.
 * ============================================================ */
(function (global) {
  'use strict';

  const LESSON_DATA = {
    id: 'extreme-wide-shot',
    unit: 'unit-01-shot-size',
    // TODO: fill in from Lesson01-ExtremeWideShot-Spec-v1.0.md once available.
  };

  global.LESSON_DATA = LESSON_DATA;
})(window);
